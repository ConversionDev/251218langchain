# 이력서 PDF

포트폴리오 네비 "Resume" 클릭 시 `/resume` 페이지가 이 폴더의 PDF를 iframe으로 띄웁니다.

- **현재 파일**: `강경구(이력서).pdf` → URL `/resume/강경구(이력서).pdf`
- 파일을 교체할 때는 같은 파일명으로 덮어쓰거나, 파일명을 바꾼 경우
  `frontend/app/resume/page.tsx`의 iframe `src`도 함께 수정해 주세요.
